# Markdown 文档规则

## 标题层级

- 生成或修改 Markdown 文件时，章节标题必须严格按层级递进：一级章节使用 `#`，其子章节使用 `##`，再下一级使用 `###`，依此类推。
- 不得跳过标题层级。例如，`#` 下不能直接出现 `###`。
- 同级章节使用相同级别的标题。

## 文件标题

- 新建 Markdown 文件时，文件名（不含扩展名）必须采用 `xx yyyyy` 格式，其中 `xx` 是从 `01` 开始递增的两位数字编号，`yyyyy` 是文件的正式标题。例如：`01 RPC调用.md`、`02 服务端缓存.md`。
- Markdown 文件名承担文档标题的作用，不要在文件内容中重复添加同名标题。
- 文件正文应直接从第一个一级章节 `#` 开始；如果有学习目标、摘要或引用说明，可以放在第一个一级章节之前，但不得将文件标题再次写成章节。
- 章节不需要带入序号。

## 面试题

- 生成面试题内容时，每道面试题的题目必须作为标题，其标题级别取决于父级标题：题目标题应比所属父级标题低一级。例如，父级标题使用 `##` 时，题目标题使用 `###`。
- 题目标题开头必须标注优先级：`🌟🌟🌟` 表示高频，`🌟🌟` 表示出现次数一般，`🌟` 表示低频，无星表示低优先级。
