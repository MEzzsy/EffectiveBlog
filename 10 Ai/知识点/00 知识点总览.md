# 基础与概念

## 工具调用

### 什么是工具调用（Tool Calling / Function Calling）？

工具调用就是模型按照定义好的格式，告诉应用它想调用哪个工具、传什么参数，真正的执行还是由应用完成。



### 如何设计可靠的工具描述和参数模式？

> 工具调用就是模型按照定义好的格式，告诉应用它想调用哪个工具、传什么参数，真正的执行还是由应用完成。工具设计要做到职责单一、名称和描述清楚、参数类型和必填项明确。服务端还必须再次校验参数和权限，写操作最好支持预览、确认和幂等。

工具调用是指向模型提供一组可用工具及其参数定义，让模型根据任务生成结构化的调用请求。应用程序接收请求后进行校验和授权，真正执行函数或外部 API，再将执行结果返回给模型。模型提出工具调用并不等于模型本身执行了工具。

可靠的工具设计应注意以下几点：

- 工具名称应明确表达单一职责，避免一个工具承担过多互不相关的操作。
- 描述要说明工具能做什么、不能做什么、何时使用，以及与相似工具的区别。
- 参数使用严格 Schema，明确类型、必填项、枚举、格式、取值范围和默认行为。
- 避免含义模糊的布尔参数，优先使用语义清晰的枚举，例如将 `force: true` 改成明确的执行模式。
- 将高风险的读操作和写操作拆成不同工具，并让写操作支持预览、确认、幂等键和审计。
- 工具结果应采用稳定的结构，包含状态、数据、错误码、可重试标志和必要的来源信息。
- 服务端必须重新校验模型生成的所有参数，并基于当前用户身份执行权限检查。

此外，应该用正常输入、歧义输入、缺失参数、越权请求和工具失败等测试用例评估工具选择与参数生成，而不能只验证理想路径。

### 工具调用失败怎么办？

工具调用失败处理的核心是“结构化错误、分类重试、幂等保护、有限降级和全链路可观测”，而不是简单地让 Agent 再试一次。



第一步是错误分类：

- 参数错误：模型生成的参数不符合 JSON Schema、缺少必填字段或业务校验失败。
- 临时性错误：网络超时、限流、服务不可用、HTTP 5xx。
- 永久性错误：工具不存在、权限不足、认证失效、资源不存在。
- 业务错误：例如余额不足、库存不足或当前状态不允许操作。
- 结果不确定：请求超时，但无法确认工具实际上有没有执行成功。

第二步是分类处理：

- 参数错误：把结构化错误反馈给 Agent，让它在限定次数内修正参数；同时通过严格 Schema、枚举和服务端校验减少错误。
- 临时性错误：使用有限次数的指数退避重试，并加入随机抖动；对限流遵循 `Retry-After`，同时设置超时、熔断和整体执行预算。
- 永久性错误：不重试，改用备用工具、重新规划任务，或者明确告知用户需要授权。
- 业务错误：将可理解、可操作的错误返回给 Agent，让它调整方案，而不是重复同一个调用。
- 结果不确定：先根据请求 ID 或操作 ID查询执行状态，再决定是否重试，避免重复下单、重复付款等问题。



## 结构化输出

 [结构化输出](结构化输出.md) 



## Agent 的自主性应如何定义和约束？

> 自主性就是 Agent 能在多大范围内自己决定怎么做、调用什么工具，以及能不能直接执行。约束它时要遵循最小权限原则，再加上最大步数、超时、预算、参数校验和审计。查询类、可回滚的操作可以更自动化，付款、删除这类高风险操作则必须人工确认。

Agent 的自主性可以定义为：在多大范围内，它能够在没有逐步人工指令的情况下，自行决定目标的执行路径、选择工具并实施操作。自主性不是简单的“开启或关闭”，而是由多个维度共同决定，包括可访问的数据、可用工具、操作权限、执行时间、成本预算、是否允许对外产生影响以及哪些步骤需要审批。

约束自主性通常采用分层机制：

- 通过明确的目标、范围和完成标准限制任务边界。
- 根据最小权限原则，只授予当前任务必需的数据和工具权限。
- 将只读、可逆写入和不可逆操作划分不同风险等级。
- 对高风险操作设置预览、二次确认、人工审批或双人复核。
- 使用确定性的参数校验、策略引擎和沙箱，而不是只依赖系统提示词。
- 设置最大步数、超时、调用配额、费用上限、重试次数和紧急停止开关。
- 保存完整审计轨迹，并提供回滚、补偿和人工接管能力。

合理的设计原则是让自主程度与任务风险匹配：风险越低、操作越可逆、验证越充分，Agent 可以拥有越高自主性；风险越高，则越需要确定性约束和人类在环。



## 如何解释 Agent 的确定性、可控性与灵活性之间的权衡？

> 固定规则最确定、最好控制，但遇到预设之外的情况就不够灵活；LLM 驱动的 Agent 更灵活，但结果和执行路径可能不稳定。实际工程里一般采用混合方式：让 LLM 处理理解和决策，让工作流、权限系统和规则代码控制边界。目标不是完全消除不确定性，而是让不确定性可验证、可追踪、可回滚。

- **确定性**是指相同输入在相同状态下得到一致执行路径和结果的程度；
- **可控性**是指系统能否把行为限制在权限、规则、预算和业务目标范围内；
- **灵活性**是指系统面对新情况、模糊需求和非预期中间结果时调整策略的能力。

固定规则和预编排工作流具有较高确定性与可控性，但只能处理预先定义的路径。由 LLM 动态决策的 Agent 灵活性更强，却可能产生不同计划、错误参数、幻觉或不可预测的调用链。三者通常无法同时最大化。

工程上常用混合架构取得平衡：让 LLM 负责意图理解、方案生成和低风险决策；让状态机、Schema、权限系统、策略引擎和业务代码控制执行边界；对关键操作增加审批；用评测集、监控和审计持续验证结果。还可以按任务风险动态调整自主性，例如低风险查询允许自动执行，高风险写操作只允许 Agent 生成计划并等待确认。

因此，目标不是让 Agent 的每一步都完全确定，而是让其变化发生在可接受、可验证、可追溯和可恢复的范围内。



# 规划与推理

1. 面对一个复杂任务时，Agent 如何进行任务拆解？
2. 如何避免 Agent 的计划过度细化或过于粗糙？
3. 当执行结果与原计划不一致时，Agent 应如何重规划？
4. 如何为 Agent 设计停止条件，避免无限循环？
5. 如何识别并处理 Agent 在推理中出现的幻觉、重复尝试或目标漂移？
6. Chain-of-Thought、ReAct 与 Reflection 在工程系统中分别有哪些优缺点？
7. 如何设计一个 Agent，使其在信息不足时主动澄清，而不是自行编造前提？
8. 如何让 Agent 在多个可选工具之间作出合理选择？
9. 如何处理长任务中的上下文膨胀问题？
10. 如果 Agent 需要执行高风险操作，规划阶段应增加哪些检查？

# 记忆与知识检索

1. 短期记忆、长期记忆、工作记忆和外部知识库有什么区别？
2. 哪些信息值得写入长期记忆？如何避免把错误或过时信息固化？
3. RAG 在 Agent 系统中解决了什么问题？它不能解决什么问题？
4. 如何设计检索、重排序和上下文组装流程？
5. 如何评估检索质量，以及检索质量对最终任务成功率的影响？
6. 向量检索、关键词检索和混合检索分别适合什么场景？
7. 如何应对知识库中的冲突内容、权限隔离和版本变化？
8. 如何让 Agent 在引用外部资料时保留来源并支持追溯？
9. 如何设计用户偏好记忆，并满足删除、更正和最小化存储的要求？
10. 何时应当选择不检索，直接回答或请求用户补充信息？

# 工具与执行

1. 如何为数据库查询、文件操作、浏览器自动化等工具划分权限？
2. 为什么工具调用必须进行参数校验？应在哪些层完成校验？
3. 如何设计具备幂等性的工具，避免 Agent 重试造成重复扣费或重复写入？
4. 工具执行失败时，应如何向 Agent 返回可用于恢复的错误信息？
5. 如何区分可安全自动重试的错误与必须人工介入的错误？
6. 如何为耗时工具调用设计超时、取消和异步任务机制？
7. 如何让 Agent 在执行写操作前展示计划并等待确认？
8. 如何记录工具调用审计日志，同时避免泄露敏感数据？
9. 如何处理外部 API 的限流、配额与成本控制？
10. 如何测试 Agent 是否正确选择、调用和解释工具结果？

# 多 Agent 系统

1. 什么场景适合采用多 Agent，而不是单 Agent 加工具？
2. 如何划分规划、执行、审查和领域专家等 Agent 的职责边界？
3. 多 Agent 之间如何传递上下文，才能避免信息丢失和上下文冗余？
4. 如何选择中心化编排、去中心化协作或层级委派架构？
5. 多 Agent 出现意见冲突时，如何进行仲裁？
6. 如何避免多个 Agent 重复劳动或相互等待造成死锁？
7. 如何控制多 Agent 系统的调用次数、延迟与成本？
8. 多 Agent 的共享记忆应如何设计权限、隔离和一致性？
9. 如何评估多 Agent 架构相较单 Agent 的真实收益？
10. 请设计一个多 Agent 系统来完成竞品调研，并说明其协作流程。

# 评估与可观测性

1. 评估 AI Agent 时，除了最终答案正确率，还应关注哪些指标？
2. 如何构建覆盖正常路径、边界条件和失败路径的评测集？
3. 什么是任务成功率、工具成功率、轨迹质量和人工接管率？
4. 如何评估一个 Agent 的规划质量？
5. 如何用离线评测、在线 A/B 实验和人工评审共同验证系统效果？
6. 如何为 Agent 的每一步决策建立可观测性？
7. 什么信息应进入 Trace？如何关联用户请求、模型调用与工具调用？
8. 如何定位「最终结果错误」究竟来自检索、规划、模型还是工具执行？
9. 如何防止评测集被提示词或流程过拟合？
10. Agent 上线后发现成功率下降，应如何排查和回归验证？

# 安全、可靠性与合规

1. Prompt Injection 是什么？在 Agent 使用网页、邮件或文档内容时如何防护？
2. 如何防止不可信内容诱导 Agent 调用高权限工具？
3. 如何设计最小权限、审批流和人类在环机制？
4. Agent 处理个人信息、商业机密时需要考虑哪些数据安全措施？
5. 如何避免模型输出敏感信息或跨用户数据？
6. 如何应对工具返回内容中包含恶意指令的情况？
7. 什么是 Jailbreak？它与 Prompt Injection 有何区别？
8. 如何设计 Agent 的安全策略，使正常业务能力不过度受损？
9. 如何为高风险操作设计回滚、补偿和紧急停止机制？
10. 如何对 Agent 系统进行红队测试？

# 系统设计与实战

1. 请设计一个客服 Agent，能够查询订单、处理退款并将复杂问题转交人工。
2. 请设计一个代码修复 Agent，并说明如何验证其修改不会引入回归。
3. 请设计一个数据分析 Agent，使其能从自然语言生成查询、解释结果并避免误操作生产库。
4. 请设计一个企业知识助手，并说明如何实现权限感知检索。
5. 请设计一个研究型 Agent，能够搜集资料、交叉验证并生成带引用的报告。
6. 如果 Agent 的响应延迟从 3 秒升至 20 秒，你会从哪些方面优化？
7. 如果 Agent 的单次任务成本过高，你会如何分析和降低成本？
8. 如何为 Agent 设计灰度发布、版本管理与回滚策略？
9. 如何处理模型供应商不可用、工具服务异常或检索服务降级？
10. 请分享一个你认为最容易被忽视的 Agent 工程风险，以及你的应对方案。
