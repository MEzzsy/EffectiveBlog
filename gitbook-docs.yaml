$schema: https://api.gitbook.com/gitbook-docs.yaml
site:
  title: Mezzsy is here
  structure:
    - type: space
      # Keep this key stable after the first Git Sync import.
      key: effectiveblog
      title: EffectiveBlog
      path: effectiveblog
      default: true
      content:
        directory: ./
        language: zh
